<div class="row">
  <div class="col-lg-12">
    <h1>ユーザー情報一覧</h1>
  </div>
</div>
<div class="row">
  <div class="col-lg-6">
    <p>ユーザー情報が表示されます。</p>
  </div>
  <div class="col-lg-6">
    <%= link_to '顧客登録', new_event_path, class: "btn btn-default pull-right" %>
  </div>
  
  <table class="table table-striped">
    <thead>
      <th>ニックネーム</th>
      <th>性別</th>
      <th>Eメール</th>
      <th>年齢</th>
      <th>地域</th>
      <th>身長</th>
      <th>体重</th>
      <th>リンク</th>
    </thead>
    <tbody>
      <% @events.each do |event| %>
        <tr>
          <td><%= event.nickname %></td>
          <td><%= event.sex %></td>
          <td><%= event.email %></td>
          <td><%= event.age%></td>
          <td><%= event.area_id %></td>
          <td><%= event.height %></td>
          <td><%= event.weight %></td>
          <td><%= link_to '詳細', event, class: "btn btn-default btn-xs" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
