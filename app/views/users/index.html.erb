<div class="row">
  <div class="col-lg-12">
    <h1>ユーザー情報一覧</h1>
  </div>
</div>
<div class="row">
  <div class="col-lg-6">
    <p>ユーザー情報が表示されます。</p>
  </div>

  <table class="table table-striped">
    <thead>
      <th>ニックネーム</th>
      <th>性別</th>
      <th>Eメール</th>
      <th>年齢</th>
      <th>地域</th>
      <th>身長</th>
      <th>体重</th>
      <th>リンク</th>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.nickname %></td>
          <td><%= user.sex %></td>
          <td><%= user.email %></td>
          <td><%= user.age%></td>
          <td><%= user.area_id %></td>
          <td><%= user.height %></td>
          <td><%= user.weight %></td>
          <td><%= link_to '詳細', user, class: "btn btn-default btn-xs" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
